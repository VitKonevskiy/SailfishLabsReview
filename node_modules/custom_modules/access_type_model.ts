let mongoose = require('mongoose')

let accessSchema = new mongoose.Schema({
    _id: mongoose.Schema.Types.ObjectId,
    accessType: String
  });
module.exports = mongoose.model('AccessType', accessSchema)